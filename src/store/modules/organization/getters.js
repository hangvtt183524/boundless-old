export const organizations = (state) => state.organizations
export const hasOrganization = (state) => state.organizations.length > 0
export const selectedOrgPermissions = (state) => state.selectedOrgPermissions
export const splashPages = (state) => state.splashPages
