export const selectedNodePermissions = (state) => state.selectedNodePermissions
export const allNodes = (state) => state.allNodes
export const sponsorRequestList = (state) => state.sponsorRequestList
